name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
# Jeden deploy na raz — drugi kanał (gałąź gh-pages) jest wyłączony
concurrency:
  group: github-pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install --no-audit --no-fund
      - run: npm test
      - run: npm run build
        env:
          VITE_CESIUM_ION_KEY: ${{ secrets.VITE_CESIUM_ION_KEY }}
          VITE_CESIUM_ION_KEYS: ${{ secrets.VITE_CESIUM_ION_KEYS }}
          VITE_GOOGLE_MAPS_KEY: ${{ secrets.VITE_GOOGLE_MAPS_KEY }}
          VITE_GOOGLE_TILES_KEYS: ${{ secrets.VITE_GOOGLE_TILES_KEYS }}
          VITE_TRAFFIC_API_BASE: ${{ vars.VITE_TRAFFIC_API_BASE }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
